# Template: docs/release.md

Describe how the TARGET project is released (web app, CLI, libraries, deploy targets), based on CI/deploy configs found in Phase 0. Even one accurate sentence is fine. Do not speculate.

---

```md
# Releases

<How this project ships. Examples:>

<!-- Web app:
"<Project> is released as a web application deployed on <platform>. Every
merge to main deploys to production; PRs get preview deployments." -->

<!-- Library:
"<Project> is published to npm as `<package>`. Releases are cut with
<tool/process>; see <workflow file>." -->

<!-- Unknown/none:
"There is currently no formal release pipeline. <Current reality>." -->
```
