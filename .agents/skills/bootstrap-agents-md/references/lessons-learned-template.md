# Template: docs/agents/lessons-learned.md

Seed the file with the content below. Include 0-5 real lessons discovered during Phase 0; if none, keep the file with just the header and section stub.

Rules for the file (enforce via AGENTS.md):

- Lessons are short, concrete, actionable bullets — "when X, do Y / avoid Z", not essays
- Group by subsystem; add `##` sections as domains accumulate lessons
- Never copy lessons from another product that don't apply here

---

```md
# Lessons Learned

Hard-won knowledge from building this codebase. When you make a mistake or discover a non-obvious behavior, add it here.

## General / Tooling

- <0-5 real lessons discovered while exploring this repo, or delete this line>
```
